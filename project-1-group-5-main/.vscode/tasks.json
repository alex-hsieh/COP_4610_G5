{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build (Make)",
      "type": "shell",
      "command": "make",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": [
        "$gcc"
      ]
    },
    {
      "label": "Clean",
      "type": "shell",
      "command": "make clean",
      "options": {
        "cwd": "${workspaceFolder}"
      }
    },
    {
      "label": "Run Shell",
      "type": "shell",
      "dependsOn": "Build (Make)",
      "command": "./bin/shell",
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}